{
  "name": "testing-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress-test":"npx cypress run --spec cypress/integration/Test_cases/*.spec.js --headed"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^5.5.0"
  },
  "devDependencies": {
    "cypress-xpath": "^1.6.0"
  }
}
